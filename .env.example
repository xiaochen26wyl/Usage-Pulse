# Optional: override Claude Code OAuth token (otherwise read from keychain / ~/.claude)
CLAUDE_CODE_OAUTH_TOKEN=your_claude_code_oauth_token_here

# Optional: custom Claude config directory
CLAUDE_CONFIG_DIR=your_claude_config_dir_here
