import React from "react";
import ReactDOM from "react-dom/client";
import { SessionSummaryApp } from "./SessionSummaryApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SessionSummaryApp />
  </React.StrictMode>
);
