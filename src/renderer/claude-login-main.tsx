import React from "react";
import ReactDOM from "react-dom/client";
import { ClaudeLoginApp } from "./ClaudeLoginApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ClaudeLoginApp />
  </React.StrictMode>
);
